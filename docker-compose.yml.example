version: '2'
services:
  web:
    build: .
    command: npm run dev
    volumes:
      - ./application:/usr/app/
      - /usr/app/node_modules
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: postgres://todoapp@postgres/todos
      PORT_APP: 3000